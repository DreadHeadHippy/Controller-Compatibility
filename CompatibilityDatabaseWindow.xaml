<Window x:Class="ControllerCompatibility.CompatibilityDatabaseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Controller Compatibility Database" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Controller Compatibility Database" FontSize="18" FontWeight="Bold" Margin="10"/>

        <DataGrid Grid.Row="1" x:Name="CompatibilityDataGrid" Margin="10" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Game Name" Binding="{Binding GameName}" Width="*"/>
                <DataGridTextColumn Header="Support Level" Binding="{Binding SupportLevel}" Width="120"/>
                <DataGridTextColumn Header="Controller Types" Binding="{Binding ControllerTypes}" Width="150"/>
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button x:Name="RefreshButton" Content="Refresh" Margin="0,0,10,0" Padding="10,5"/>
            <Button x:Name="CloseButton" Content="Close" Padding="10,5" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>